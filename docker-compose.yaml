version: '3.4'

services:
  app:
    build:
      context: .
      target: base
    working_dir: /app
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - ./:/app
    logging:
      options:
        max-size: "50m"

  db:
    image: postgres:15
